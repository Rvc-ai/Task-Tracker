<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">

        <h1 class="text-green-500 text-3xl">Task Tracker</h1>
        <form action="/create" method="post">
          <input
            required
            class="block w-full rounded-lg outline-none px-5 py-2 bg-zinc-800 mt-5"
            type="text"
            placeholder="Task title"
            name="title"
          /><textarea
            placeholder="Task details"
            class="block px-5 w-full rounded-lg outline-none py-2 bg-zinc-800 resize-none mt-5"
            name="details"
          ></textarea>
          <input
            class="mt-5 bg-blue-700 px-5 py-2 rounded-lg"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex flex-wrap gap-3">
        <% if(files.length > 0) { %> <% files.forEach((val)=>{ %>
        <div
          class="task w-72 px-5 py-6 rounded-lg bg-zinc-800 shadow-lg hover:bg-zinc-700 transition-all duration-200"
        >
          <h1 class="text-white text-2xl tracking-tighter truncate">
            <%= val %>
          </h1>
          <a class="text-blue-400 inline-block mt-3" href="/file/<%= val %>"
            >Read more</a
          >
          <form
            action="/delete/<%= val %>"
            method="post"
            class="inline-block mt-3"
          >
            <button
              type="submit"
              class="bg-red-600 px-3 py-1 rounded-md text-white"
            >
              Delete
            </button>
          </form>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-zinc-600 text-xl">no tasks yet</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
